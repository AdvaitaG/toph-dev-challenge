# Recent updates

- Added public employee signup and a separate employee activity page. Employee submissions are saved in Supabase and appear on the manager dashboard.
- Added manager-only removal for employee-submitted activities. The original Figma demo logs remain protected.
- Fixed valid field IDs being rejected during submission, and added handling for email-confirmation codes and clearer signup errors.
- Created the Anthony Wells demo employee account and reset the confirmed AGP employee account password at the user's request. Passwords are not stored in this repository.
- Verified the employee/manager flow against the linked Supabase project. Lint, type checks, and all 21 browser tests passed after the confirmation-flow change.
- Added Google sign-in to the login and employee-signup pages, with a PKCE callback. Applied the version-controlled employee-provisioning migration to the linked Supabase project. New Google users get Bays Ranch employee profiles; the existing confirmed manager account retains its manager role when Google links to the same email. The linked project's Google provider is currently disabled; its OAuth client ID, secret, and redirect URLs still need configuration in Supabase/Google before live sign-in works.
- Updated dashboard metrics: Today’s Recordings counts employee submissions made on the farm’s current local date and falls when the manager deletes one. Active Workers counts the four Figma workers plus active employees with accounts, including AGP and Anthony. Response Accuracy remains the scored demo snapshot average. Applied the version-controlled manager profile-read policy; linked data currently computes 0 / 6 / 90.
- Added employee voice submissions: browser audio capture, automatic speech recognition where supported, an editable transcript, and an optional written note. The manager can play saved audio from a private signed URL and remove employee submissions. Applied the version-controlled `20260917050000_employee_voice_recordings.sql` migration to the linked project. Local SQL/RLS checks, Chrome submission/playback tests, and a reversible live Supabase upload-to-playback check passed. Browser transcription depends on browser support and may require a network connection; a server transcription provider is not configured.
- Repaired **This Month** to use the farm's current calendar month instead of the April demo snapshot. The dashboard starts unfiltered so the Figma rows remain visible. Added a manager-only Employees tab with all farm workers, real contact emails copied from Auth where available, and an inline way to add missing contact addresses. Applied `20260917060000_employee_contact_emails.sql` to the linked project; seed workers without supplied emails remain explicitly blank.

No manager email notification service was implemented; that request was withdrawn.
